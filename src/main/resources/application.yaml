spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/goods_store2
    username: postgres
    password: postgres
#spring:
#  datasource:
#    url: jdbc:postgresql://db:5432/goods_store2
#    username: postgres
#    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true